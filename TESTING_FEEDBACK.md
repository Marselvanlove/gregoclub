# 🧪 План тестирования Feedback System

**Тестировщик:** Админ ID `614771593`  
**Дата:** 26.02.2026

---

## Подготовка

### 1. Создать тестовую встречу

Через админ-панель создать запланированную рассылку:
- Сегмент: `active` (или ваш уровень)
- Добавить RSVP-кнопки: `Да`
- Название события: `Тестовая встреча — Feedback System`
- Дата/время события: **текущее время + 5 минут** (для быстрого теста)
- Zoom-ссылка: `https://zoom.us/test` (любая)

### 2. Записаться на встречу

- Нажать кнопку "Я буду" в полученном сообщении
- Убедиться, что статус RSVP сохранён

---

## 🎯 Тест 1: Отправка опроса (автоматическая)

**Что проверяем:** Опрос приходит через 110 минут после `rsvp_event_datetime`

**Шаги:**
1. Создать встречу на **текущее время + 5 минут**
2. Записаться на неё ("Я буду")
3. Подождать **5 мин (встреча) + 90 мин (длительность) + 20 мин (задержка) = 115 минут**

**⚠️ Для быстрого теста:**
Можно временно изменить логику в `database/requests.py` → `get_broadcasts_needing_feedback()`:
```python
# Вместо:
feedback_window_start = now + timedelta(minutes=109)
feedback_window_end = now + timedelta(minutes=111)

# Поставить:
feedback_window_start = now + timedelta(minutes=4)  # 5 мин - 1 мин
feedback_window_end = now + timedelta(minutes=6)    # 5 мин + 1 мин
```

**Ожидаемый результат:**
- Через ~5 минут после создания встречи приходит сообщение с текстом `FEEDBACK_INTRO`
- Есть кнопка "📝 Оставить отзыв"

---

## 🎯 Тест 2: Прохождение опроса

**Что проверяем:** Пошаговое заполнение опроса

**Шаги:**
1. Нажать "📝 Оставить отзыв"
2. Ожидается вопрос 1: "Оцените встречу" с кнопками 1-5
3. Нажать любую оценку (например, `5`)
4. Ожидается вопрос 2: "Комфортен ли был уровень?" с 3 кнопками
5. Нажать "✅ Да, идеально"
6. Ожидается вопрос 3: "Придёте на следующую встречу?" с 3 кнопками
7. Нажать "🔥 Да"
8. Ожидается финальное сообщение: `FEEDBACK_THANKS` с кнопкой "📅 Расписание"

**Ожидаемый результат:**
- Плавный переход между вопросами
- Финальное сообщение после 3-го вопроса
- В БД сохранён feedback с `rating=5`, `level_comfort=perfect`, `will_attend_next=yes`

---

## 🎯 Тест 3: Проверка БД

**Что проверяем:** Сохранение ответов

**Шаги:**
1. После прохождения опроса проверить БД:
```sql
SELECT * FROM event_feedbacks WHERE telegram_id = 614771593 ORDER BY created_at DESC LIMIT 1;
```

**Ожидаемый результат:**
- Запись существует
- `rating = 5`
- `level_comfort = 'perfect'`
- `will_attend_next = 'yes'`
- `followup_sent_at = NULL` (пока)

---

## 🎯 Тест 4: Follow-up для высокой оценки

**Что проверяем:** Сообщение через 1-2 дня после высокой оценки

**⚠️ Для быстрого теста:**
Изменить `get_feedbacks_for_followup()` в `database/requests.py`:
```python
# Вместо:
created_before = now - timedelta(days=days_since_feedback)
created_after = now - timedelta(days=days_since_feedback + 1)

# Поставить:
created_before = now - timedelta(minutes=1)
created_after = now - timedelta(minutes=5)
```

**Шаги:**
1. Пройти опрос с оценкой 4 или 5
2. Подождать ~6 часов (или изменить интервал scheduler на 1 минуту для теста)

**Ожидаемый результат:**
- Приходит сообщение: `FEEDBACK_FOLLOWUP_HIGH_RATING`
- Есть кнопка "📅 Расписание"
- В БД `followup_sent_at` обновлён

---

## 🎯 Тест 5: Follow-up "было сложно"

**Что проверяем:** Предложение уровня ниже для B1-C пользователей

**Шаги:**
1. Убедиться, что у админа `language_level = 'B1-B2'` или `'B1-C'`
2. Пройти опрос с ответом "🤔 Немного сложно"
3. Подождать выполнения scheduler

**Ожидаемый результат:**
- Приходит сообщение: `FEEDBACK_FOLLOWUP_HARD_LEVEL`
- Текст предлагает уровень A1-A2

---

## 🎯 Тест 6: Follow-up "не приду"

**Что проверяем:** Напоминание через неделю

**⚠️ Для быстрого теста:**
Изменить `get_feedbacks_not_coming_for_followup()`:
```python
# days_since_feedback=7 → days_since_feedback=0
created_before = now - timedelta(minutes=1)
```

**Шаги:**
1. Пройти опрос с ответом "❌ Нет"
2. Подождать выполнения scheduler

**Ожидаемый результат:**
- Приходит: `FEEDBACK_FOLLOWUP_NOT_COMING`

---

## 🎯 Тест 7: Edge cases

### 7.1 Повторный ответ на опрос
**Шаги:**
1. Пройти опрос до конца
2. Попробовать нажать кнопку из старого сообщения

**Ожидаемый результат:**
- Callback обрабатывается корректно (перезаписывает предыдущий ответ)

### 7.2 Встреча без участников
**Шаги:**
1. Создать встречу
2. НЕ записываться на неё ("Я буду")
3. Дождаться времени отправки опроса

**Ожидаемый результат:**
- Опрос НЕ отправляется никому
- В логах: "Нет участников для опроса рассылки #X"

### 7.3 Неполный ответ
**Шаги:**
1. Начать опрос
2. Ответить только на 1-й вопрос
3. Закрыть бота
4. Снова нажать кнопку из старого сообщения

**Ожидаемый результат:**
- Можно продолжить с того же места (или начать заново)

---

## 📝 Чеклист проверки

- [ ] Опрос приходит через 110 минут после начала встречи
- [ ] Опрос приходит только участникам (attending)
- [ ] Все 3 вопроса отображаются корректно
- [ ] Ответы сохраняются в БД
- [ ] Follow-up для 4-5 звёзд работает
- [ ] Follow-up для "сложно" работает (для B1-C)
- [ ] Follow-up для "не приду" работает
- [ ] Встреча без участников не вызывает ошибок
- [ ] `feedback_sent` флаг предотвращает повторную отправку

---

## 🚀 Быстрый запуск теста (для разработки)

**1. Изменить время проверки на 5 минут вместо 110:**

```python
# database/requests.py → get_broadcasts_needing_feedback()
feedback_window_start = now + timedelta(minutes=4)
feedback_window_end = now + timedelta(minutes=6)
```

**2. Изменить интервал scheduler на 10 секунд:**

```python
# services/scheduler.py → create_scheduler()
scheduler.add_job(
    send_post_event_feedback,
    trigger=IntervalTrigger(seconds=10),  # вместо minutes=1
    ...
)
```

**3. Перезапустить бота:**

```bash
docker-compose restart
```

**4. Создать встречу на +5 минут от текущего времени**

**5. Записаться "Я буду"**

**6. Подождать ~5-6 минут → опрос придёт автоматически**

---

## 📊 Результаты тестов

| Тест | Статус | Комментарии |
|------|--------|-------------|
| Отправка опроса | ⏳ | |
| Прохождение опроса | ⏳ | |
| Сохранение в БД | ⏳ | |
| Follow-up 4-5⭐ | ⏳ | |
| Follow-up "сложно" | ⏳ | |
| Follow-up "не приду" | ⏳ | |
| Edge cases | ⏳ | |
